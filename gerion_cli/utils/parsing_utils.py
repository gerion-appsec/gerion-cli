def parse_secrets_tool_output(output):
    # # Parse the output and format it as needed
    # results = []
    # for line in output.splitlines():
    #     if "leak" in line:
    #         results.append(line)
    # return "\n".join(results)
    return output